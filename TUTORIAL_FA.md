# راهنمای کامل نصب — مخصوص کسانی که تا حالا کدنویسی نکرده‌اند

این راهنما فرض می‌کند شما **هیچ** تجربه‌ی قبلی با ترمینال، Git یا برنامه‌نویسی ندارید. قدم‌به‌قدم جلو می‌رویم و هیچ‌چیزی را بدیهی فرض نمی‌کنیم. اگر جایی گیر کردید، همان قسمت را دوباره از اول بخوانید — احتمالاً یک قدم جا افتاده.

⏱ زمان تقریبی: ۲۰ تا ۳۰ دقیقه، فقط برای یک‌بار.

---

## قدم صفر: ترمینال چیست؟

در این راهنما چند بار از شما می‌خواهیم «دستور» را در «ترمینال» اجرا کنید. ترمینال یک پنجره‌ی متنی است که به‌جای کلیک کردن، با تایپ کردن به کامپیوترتان فرمان می‌دهید. جای نگرانی نیست — فقط باید متن را دقیقاً همان‌طور که نوشته شده تایپ (یا کپی) کنید و کلید Enter را بزنید.

**چطور ترمینال را باز کنم؟**

- **ویندوز:** دکمه‌ی Start را بزنید، بنویسید `PowerShell` و Enter بزنید.
- **مک:** کلید `Command + Space` را بزنید، بنویسید `Terminal` و Enter بزنید.
- **لینوکس:** معمولاً کلید `Ctrl + Alt + T`.

از این به بعد هر جا نوشتیم «در ترمینال بنویسید»، یعنی همین پنجره.

---

## قدم ۱: نصب Git

Git ابزاری است که با آن کد پروژه را از GitHub دانلود می‌کنیم.

1. به آدرس [git-scm.com/downloads](https://git-scm.com/downloads) بروید.
2. نسخه‌ی مخصوص سیستم‌عاملتان (ویندوز/مک/لینوکس) را دانلود و نصب کنید — در تمام مراحل نصب فقط «Next» را بزنید، تنظیمات پیش‌فرض کافی است.
3. برای اطمینان، ترمینال را ببندید و دوباره باز کنید، سپس بنویسید:
   ```
   git --version
   ```
   اگر یک شماره نسخه (مثل `git version 2.43.0`) دیدید، یعنی درست نصب شده.

---

## قدم ۲: نصب Python

1. به آدرس [python.org/downloads](https://www.python.org/downloads/) بروید و آخرین نسخه‌ی **۳.۱۱** یا بالاتر را دانلود کنید.
2. هنگام نصب روی ویندوز، **حتماً** تیک گزینه‌ی **«Add python.exe to PATH»** را در پایین صفحه‌ی اول نصب بزنید — این قسمت خیلی مهم است و اگر جا بیفتد، مراحل بعدی کار نمی‌کنند.
3. برای اطمینان، در ترمینال بنویسید:
   ```
   python --version
   ```
   (روی مک/لینوکس ممکن است لازم باشد به‌جایش `python3 --version` بنویسید.)
   باید چیزی شبیه `Python 3.11.x` ببینید.

---

## قدم ۳: نصب Node.js

Node.js برای ساختن بخش رابط کاربری (frontend) لازم است.

1. به آدرس [nodejs.org](https://nodejs.org) بروید و نسخه‌ی **LTS** (نسخه‌ی پایدار پیشنهادی) را دانلود و نصب کنید — باز هم در تمام مراحل «Next» کافی است.
2. برای اطمینان:
   ```
   node --version
   npm --version
   ```
   باید دو شماره نسخه ببینید (مثلاً `v20.x.x` و `10.x.x`).

---

## قدم ۴: نصب uv

`uv` ابزاری است که پروژه از آن برای مدیریت وابستگی‌های پایتون استفاده می‌کند. در ترمینال بنویسید:

```
pip install uv
```

(روی مک/لینوکس اگر کار نکرد، `pip3 install uv` را امتحان کنید.)

---

## قدم ۵: دانلود پروژه

در ترمینال، اول به پوشه‌ای بروید که می‌خواهید پروژه در آن ذخیره شود (مثلاً Desktop):

```
cd Desktop
```

سپس پروژه را دانلود کنید:

```
git clone https://github.com/Retro-Zero/epub-ai-translator.git
cd epub-ai-translator
```

الان یک پوشه‌ی جدید به اسم `epub-ai-translator` روی دسکتاپ شما ساخته شده و ترمینال داخل همان پوشه است.

---

## قدم ۶: نصب Backend

این دستورها را **یکی‌یکی** اجرا کنید (بعد از هرکدام Enter بزنید و صبر کنید تمام شود):

```
uv venv --python 3.11
uv pip install --python .venv/bin/python -r backend/requirements.txt
cp backend/.env.example backend/.env
```

> اگر روی ویندوز هستید و دستور آخر (`cp`) کار نکرد، به‌جایش این را بنویسید:
> ```
> copy backend\.env.example backend\.env
> ```

این مرحله ممکن است یکی دو دقیقه طول بکشد — کاملاً عادی است.

---

## قدم ۷: ساختن Frontend

```
cd frontend
npm install
npm run build
cd ..
```

`npm install` معمولاً یکی دو دقیقه طول می‌کشد. تا وقتی نوشته «done» یا پرامپت به حالت عادی برنگشته، منتظر بمانید.

---

## قدم ۸: اجرای برنامه

```
cd backend
../.venv/bin/uvicorn app.main:app --port 8000
```

> روی ویندوز، به‌جای خط دوم بنویسید:
> ```
> ..\.venv\Scripts\uvicorn app.main:app --port 8000
> ```

اگر همه‌چیز درست پیش رفته، خطی شبیه این می‌بینید:
```
Uvicorn running on http://127.0.0.1:8000
```

**این پنجره‌ی ترمینال را باز و باز نگه دارید** — همین پنجره است که برنامه را روشن نگه می‌دارد. اگر آن را ببندید، برنامه خاموش می‌شود.

حالا مرورگرتان را باز کنید و به این آدرس بروید:

```
http://127.0.0.1:8000
```

اگر صفحه‌ی اپ را دیدید — تبریک می‌گویم، نصب تمام شد! 🎉

---

## قدم ۹: اولین تست — بدون هیچ هزینه‌ای

قبل از اینکه سراغ یک کتاب واقعی بروید، پیشنهاد می‌کنیم اول با **حالت آزمایشی (Mock mode)** آشنا شوید — این حالت هیچ هزینه‌ای ندارد و به هیچ API key نیازی نیست:

1. در برنامه، به تب **Settings** بروید.
2. کلید **Mock mode** را روشن کنید.
3. به صفحه‌ی اول برگردید و دکمه‌ی **«کتاب نمونه»** را بزنید.
4. کل مسیر — Glossary، ترجمه، QA، دانلود — را با یک کتاب آزمایشی طی کنید تا با کار کردن اپ آشنا شوید.

هر خروجی این حالت با علامت **【آزمایشی】** مشخص می‌شود، پس هیچ‌وقت با ترجمه‌ی واقعی اشتباه نمی‌شود.

---

## قدم ۱۰: گرفتن API key واقعی (وقتی آماده‌ی ترجمه‌ی واقعی بودید)

برای ترجمه‌ی واقعی، به یک کلید API از یکی از این سرویس‌ها نیاز دارید. ارزان‌ترین و پیشنهادی‌ترین گزینه فعلاً **DeepSeek** است:

1. به [platform.deepseek.com](https://platform.deepseek.com) بروید و ثبت‌نام کنید.
2. از بخش API Keys، یک کلید جدید بسازید و آن را کپی کنید (این کلید فقط یک‌بار کامل نمایش داده می‌شود، جایی ذخیره‌اش کنید).
3. در اپ، به **Settings** بروید، Mock mode را خاموش کنید، Provider را روی **DeepSeek** بگذارید و کلید را در فیلد API Key وارد کنید.
4. دکمه‌ی **Test connection** را بزنید تا مطمئن شوید کلید درست کار می‌کند.

این کلید فقط روی کامپیوتر خودتان ذخیره می‌شود و به هیچ سرور دیگری (از جمله سازنده‌ی این پروژه) ارسال نمی‌شود.

---

## قدم ۱۱: ترجمه‌ی اولین کتاب واقعی‌تان

1. فایل `.epub` انگلیسی خود را در صفحه‌ی اول آپلود کنید.
2. اطلاعات اولیه‌ی کتاب (عنوان، تعداد فصل‌ها) را بررسی کنید.
3. Glossary پیشنهادی را مرور و تأیید کنید (می‌توانید هر ترجمه را ویرایش کنید).
4. دکمه‌ی ترجمه‌ی کل کتاب را بزنید و پیشرفت را دنبال کنید.
5. اختیاری: مرحله‌ی QA را اجرا کنید تا مشکلات احتمالی را ببینید و اصلاح کنید.
6. روی **Finalize** بزنید و فایل EPUB فارسی نهایی را دانلود کنید.

---

## دفعه‌های بعد — چطور دوباره برنامه را اجرا کنم؟

لازم نیست همه‌ی مراحل بالا را تکرار کنید. فقط کافی است:

```
cd Desktop/epub-ai-translator/backend
../.venv/bin/uvicorn app.main:app --port 8000
```

و دوباره مرورگر را روی http://127.0.0.1:8000 باز کنید.

---

## مشکلات رایج

**«python is not recognized» یا «command not found»**
یعنی Python درست نصب نشده یا به PATH اضافه نشده. نصب Python را دوباره انجام دهید و حتماً تیک «Add to PATH» را بزنید (قدم ۲).

**«port 8000 already in use» یا خطای مشابه**
یعنی یک نسخه‌ی دیگر از برنامه از قبل روشن است. یا آن پنجره‌ی ترمینال قدیمی را ببندید، یا پورت دیگری امتحان کنید:
```
../.venv/bin/uvicorn app.main:app --port 8001
```
و بعد به‌جای 8000 از 8001 در مرورگر استفاده کنید.

**صفحه در مرورگر باز نمی‌شود**
مطمئن شوید همان پنجره‌ی ترمینالی که `uvicorn` در آن اجرا شده، هنوز باز است و خطای قرمزی نشان نداده.

**هنوز گیر کرده‌اید؟**
یک Issue در [صفحه‌ی GitHub پروژه](https://github.com/Retro-Zero/epub-ai-translator/issues) باز کنید و دقیقاً بنویسید در کدام قدم و با چه پیامی گیر کرده‌اید.
